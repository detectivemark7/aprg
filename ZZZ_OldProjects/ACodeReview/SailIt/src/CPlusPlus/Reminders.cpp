// limit variable scope
// use constructor instead of assignment
// intialize parameter
// check initialvalue for parameters
// isempty instead of "" or zero
// check using and includes if used
// list of constexpr variables instead of const//constexpr are initialize during compile time
// initializer of string should not have ""

// class order should be constructor, destructor, operators, modifiers, getters

// dont be afraid using namespace at implementation

// anti yoda if bidirectional normal operator

// switch case instead to if elseif

// set

// bool result di dapat dapat may is

// add finding with function namay "and"

// constructor instead of assignment operator

// return "" dapat// return string("")

// when to forward declare

// reminder prefer namespace function, instead of class of static functions

// reminder rule of order of parameters is the most used type to the least used type

// reminder "get" -> if output is in return type, "fill" -> if output is pass by reference
